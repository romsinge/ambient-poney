import { FilterPoniesPipe } from './filter-ponies.pipe';

describe('FilterPoniesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPoniesPipe();
    expect(pipe).toBeTruthy();
  });
});
