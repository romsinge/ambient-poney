import { BoostDirective } from './boost.directive';

describe('BoostDirective', () => {
  it('should create an instance', () => {
    const directive = new BoostDirective();
    expect(directive).toBeTruthy();
  });
});
